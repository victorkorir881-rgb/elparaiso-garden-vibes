# Copy to .env and fill in.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Canonical production URL used by Supabase auth redirects.
# Set this in Vercel → Project Settings → Environment Variables for each env
# (Production, Preview, Development) so emailed/OAuth redirects always land
# on the correct deployment.
VITE_SITE_URL="https://elparaisogardens.vercel.app"
